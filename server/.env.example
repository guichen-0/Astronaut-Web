# 数据库连接（相对于 server 目录指向项目根目录的数据库）
DATABASE_URL="file:../prisma/dev.db"

# JWT 密钥（生产环境请更换）
JWT_SECRET="change-this-to-a-random-string-in-production"

# 前端地址（CORS 允许的来源）
CORS_ORIGIN="http://localhost:3000"

# 后端端口
PORT=4000
